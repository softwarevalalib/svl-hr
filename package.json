{
  "name": "svl-hr",
  "private": true,
  "description": "SVL HRM — frontend + API on one Vercel project",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start:api": "npm start --prefix frontend/backend",
    "start:web": "npm start --prefix frontend",
    "migrate": "npm run migrate --prefix frontend/backend",
    "migrate:neon": "npm run migrate:neon --prefix frontend/backend"
  }
}
